namespace t0
{
    /// <summary>
    /// C# refresher exercises. Implement the methods marked with TODO.
    /// Run the unit tests in t0.Tests to verify your solutions.
    /// </summary>
    public static class Exercises
    {
        /// <summary>
        /// Returns the factorial of n (e.g. Factorial(5) = 120).
        /// Factorial(0) = 1 by definition.
        /// </summary>
        public static long Factorial(int n)
        {
            // TODO: Implement Factorial
            // - If n is 0 or 1, return 1.
            // - Otherwise, multiply all integers from 1 to n together.
            throw new NotImplementedException();
        }

        /// <summary>
        /// Returns the string reversed (e.g. "hello" becomes "olleh").
        /// </summary>
        public static string ReverseString(string text)
        {
            // TODO: Implement ReverseString
            // - Return the characters of the input string in reverse order.
            throw new NotImplementedException();
        }

        /// <summary>
        /// Counts the number of vowels (a, e, i, o, u) in the string.
        /// The check should be case-insensitive.
        /// </summary>
        public static int CountVowels(string text)
        {
            // TODO: Implement CountVowels
            // - Loop through each character and count how many are vowels.
            // - Both uppercase and lowercase vowels count.
            throw new NotImplementedException();
        }
    }
}
